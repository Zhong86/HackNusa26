import React from "react";
import ReactDOM from "react-dom/client";
import SentinelLoopApp from "./app";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SentinelLoopApp />
  </React.StrictMode>
);
